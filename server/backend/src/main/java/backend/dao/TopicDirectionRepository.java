package backend.dao;

import backend.domain.TopicDirection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TopicDirectionRepository extends JpaRepository<TopicDirection, Long> {
}
